apiVersion: apps/v1
kind: Deployment
metadata:
  name: blacklist-redis-depl
  labels:
    app: blacklist-redis
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: blacklist-redis
  template: 
    metadata:
      labels:
        app: blacklist-redis
    spec:
      containers:
        - name: blacklist-redis
          image: redis:latest
          ports:
            - containerPort: 27017
---
# Service
apiVersion: v1
kind: Service
metadata: 
  name: blacklist-redis-srv
spec: 
  selector:
    app: blacklist-redis
  ports:
    - name: db
      protocol: TCP
      port: 6379
      targetPort: 6379
