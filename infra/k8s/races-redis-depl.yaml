apiVersion: apps/v1
kind: Deployment
metadata:
  name: races-redis-depl
  labels:
    app: races-redis
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: races-redis
  template: 
    metadata:
      labels:
        app: races-redis
    spec:
      containers:
        - name: races-redis
          image: redis:latest
          ports:
            - containerPort: 27017
---
# Service
apiVersion: v1
kind: Service
metadata: 
  name: races-redis-srv
spec: 
  selector:
    app: races-redis
  ports:
    - name: db
      protocol: TCP
      port: 6379
      targetPort: 6379
