# ---- Positions REST API (rate-limited) ----
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: positions-api-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "3"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /api/positions/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: positions-srv
                port:
                  number: 3000
---
# ---- Auth (strict — brute force / credential stuffing risk) ----
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rpm: "5"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "2"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /api/users/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: auth-srv
                port:
                  number: 3000
---
# ---- Races ----
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: races-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rps: "30"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "3"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /api/races/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: races-srv
                port:
                  number: 3000
---
# ---- Archive ----
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archive-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rps: "10"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /api/archive/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: archive-srv
                port:
                  number: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: client-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rps: "10"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: client-srv
                port:
                  number: 80
# ---- Socket.io (no aggressive rate limiting — persistent/streaming traffic) ----
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: socket-gateway-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "socket-gateway-srv"
spec:
  ingressClassName: nginx
  tls:
    - secretName: ticket-com-tls
      hosts:
        - ticket.com
  rules:
    - host: ticket.com
      http:
        paths:
          - path: /socket.io/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: socket-gateway-srv
                port:
                  number: 3000